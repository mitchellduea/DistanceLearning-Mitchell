public class HiddenWord{
private String hiddenWord;
public String getHint( String g){
  for ( int i = 0; i <g.length(); i ++){
    if (g.substring(i+1).equals(i+1)))
  }
  else (g.substring(i+1).equals(g.substring(g.length))){
    return =+ "+"
  }
  return hiddenWord;
}

}
